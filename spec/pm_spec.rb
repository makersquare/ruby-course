require 'spec_helper'

describe "ProjectManager" do
  xit "displays menu when receiving input help" do
    expect(STDOUT).to receive(:puts).with("  mark TID - Mark task with id=TID as complete"
)
  end


end
